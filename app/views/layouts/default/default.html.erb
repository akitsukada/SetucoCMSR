<!DOCTYPE html>
<html>
    <head>
        <%= render :partial => "shared/default/html_header" %>
        <%= stylesheet_link_tag 'style' %>
        <%= javascript_include_tag :defaults %>
        <%= csrf_meta_tag %>
        <title><%= @subtitle %>SetucoCMSR</title>
    </head>
    <body>
        <!-- wrapper START -->
        <div id="wrapper">
            <!-- header START -->
            <div id="header_innerL">
                <%= render :partial => 'shared/default/header' %>
                <%#php echo $this->render('header.phtml'); %>
            </div>
            <!-- header END -->
            <!-- content START -->
            <div id="content">
                <!-- mainContent START -->
                <div id="mainContent">
                    <%= yield %>
                </div>
                <!-- mainContent END -->
                <!-- subContent START -->
                <div id="subContent">
                    <%= render :partial => 'shared/default/navi' %>
                </div>
                <!-- subContent END -->
            </div>
            <!-- content END -->
            <%= render :partial => 'shared/default/footer' %>
        </div>
        <!-- wrapper END -->
    </body>
</html>
